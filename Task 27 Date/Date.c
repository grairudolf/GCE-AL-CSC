#include <stdio.h>
#include <stdlib.h>

int main()
{
    int month, day, year;    //date read in from user
    int daysInMonth;         //number of days in the month
    int monthValid, yearValid, dayValid;   //true if input from user is valid
    int leapYear;

    printf("Enter the year, month and day\n");
    scanf("%d %d %d", &year, &month, %day)
    if(month > 12 || month < 1){
        printf("Enter a valid month\n");
    }


    return 0;
}
